apiVersion: apps/v1
kind: Deployment
metadata:
  name: openaibackend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openaibackend
  template:
    metadata:
      labels:
        app: openaibackend
    spec:
      nodeSelector:
        agentpool: agentpool
      containers:
      - name: openaibackend
        image: acropenai2.azurecr.io/backend:27

        # FIX: expose containerPort 8080
        ports:
          - containerPort: 8080

        env:
          - name: AZURE_OPENAI_ENDPOINT
            value: "https://foundry11234.openai.azure.com"
          - name: AZURE_OPENAI_DEPLOYMENT
            value: "gpt-5-nano"
          - name: REDIS_HOST
            value: "aiforredis.uksouth.redis.azure.net"
          - name: REDIS_PORT
            value: "10000"
          - name: AZURE_CLIENT_ID
            value: "946c4638-a140-4a49-9357-1531c9b2ad2e"
          


---
apiVersion: v1
kind: Service
metadata:
  name: openaibackend-svc
spec:
  type: LoadBalancer
  selector:
    app: openaibackend
  ports:
    - name: http
      port: 80
      targetPort: 8080
